import Cocoa

//Type annotation - Swift assigns each variable and constant a type based on what value it’s given when it’s created. So, when you write code like this Swift can see it holds a string:

let str = "Hello, playground" //So, when you write code like this Swift can see it holds a string.

//Type inference: Swift is able to infer the type of something based on how you created it.

//If you want you can be explicit about the type of your data rather than relying on Swift’s type inference, like this👇
let album: String = "AceView"
let year: Int = 2022 // Integers = Int
let pi: Double = 3.14
let AceViewCodes: Bool = true // Booleans = Bool
